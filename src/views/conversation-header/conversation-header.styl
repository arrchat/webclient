@import 'dimensions'
@import 'theme'

// conversation-header view
.conversation-header
  padding: dimensions.section.padding * 3  dimensions.section.padding dimensions.section.small-pad
  color: theme.text-visible
  border-bottom: 1px solid theme.gray
  text-align right
  text-transform uppercase
  background #fff
  transform translateZ(1px)
  .login
    color: theme.text
    text-transform none
