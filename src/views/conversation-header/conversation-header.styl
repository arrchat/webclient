@import 'dimensions'
@import 'theme'

// conversation-header view
.conversation-header
  margin-top: dimensions.section.padding * 3 - dimensions.section.small-pad
  padding: dimensions.section.small-pad dimensions.section.padding
  color: theme.text-visible
  border-bottom: 1px solid theme.gray
  text-align right
  text-transform uppercase
  .login
    color: theme.text
    text-transform none
